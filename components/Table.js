import React from 'react'
import styles from '../styles/'

const Table = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

const Tr = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

const Td = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default Table
